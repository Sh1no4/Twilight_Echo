{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-player-capability",
  "description": "Minimal capabilities for the Twilight Echo tray player window",
  "windows": ["tray-player"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide"
  ]
}