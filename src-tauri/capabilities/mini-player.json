{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mini-player-capability",
  "description": "Minimal capabilities for the Twilight Echo mini player window",
  "windows": ["mini-player"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-close"
  ]
}