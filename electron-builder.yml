appId: com.TwilightEcho.music
productName: TwilightEcho
directories:
  buildResources: build
asar: true
icon: build/icon.png
afterPack: scripts/after-pack-windows.cjs
files:
  - out/**
  - node_modules/**
  - package.json
  - '!out/renderer/audio-engine/**'
  - '!out/renderer/plugins/**'
  - '!out/renderer/plugin-index/**'
  # Keep packaged Nunito/Nunito Sans (latin) UI fonts; strip legacy heavy CJK/Outfit.
  - '!out/renderer/font/Alibaba-PuHuiTi-Medium.ttf'
  - '!out/renderer/font/Outfit-**'
  - '!out/renderer/font/**/*.{ttf,otf,woff}'
  - '!node_modules/**/{test,tests,__tests__,docs,doc,example,examples,benchmark,benchmarks}/**'
  - '!node_modules/**/*.map'
  - '!node_modules/**/*.ts'
  - '!node_modules/**/*.md'
  - '!node_modules/@neteasecloudmusicapienhanced/api/public/**'
extraResources:
  - from: build/icon.ico
    to: icon.ico
  - from: build/icon.png
    to: icon.png
  - from: resources/audio-engine
    to: audio-engine
    filter:
      - '**/*'
  - from: resources/plugins
    to: plugins
    filter:
      - '**/*'
  - from: resources/plugin-index
    to: plugin-index
    filter:
      - '**/*'
win:
  executableName: TwilightEcho
  icon: build/icon.ico
  # Development packages remain unsigned. The protected release config extends this file and
  # enables forceCodeSigning; see electron-builder.release-win.yml.
  signAndEditExecutable: true
  signExts:
    - .exe
    - .dll
    - .node
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: always
  createStartMenuShortcut: true
  deleteAppDataOnUninstall: false
  include: build/installer.nsh
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
