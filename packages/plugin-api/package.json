{
  "name": "@twilight-echo/plugin-api",
  "version": "0.1.0",
  "description": "Authoritative TypeScript typings for Twilight Echo plugin API v1 and v2.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "theme-contract.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.typecheck.json",
    "prepack": "pnpm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
