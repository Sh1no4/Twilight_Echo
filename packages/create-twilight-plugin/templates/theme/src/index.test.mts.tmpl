import assert from 'node:assert/strict'
import test from 'node:test'
import { activate } from './index.mjs'

test('registers theme', async () => {
  let registered = false
  await activate({
    apiVersion: 1,
    storagePath: '',
    settings: {
      get: async () => undefined,
      set: async () => undefined,
      delete: async () => undefined
    },
    logger: {
      debug: () => undefined,
      info: () => undefined,
      warn: () => undefined,
      error: () => undefined
    },
    twilight: {
      events: {} as never,
      player: {} as never,
      providers: {} as never,
      ui: {} as never,
      themes: {
        register: async () => {
          registered = true
        }
      }
    }
  })
  assert.equal(registered, true)
})
