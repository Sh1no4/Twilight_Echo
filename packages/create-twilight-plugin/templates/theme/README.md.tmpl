# {{pluginName}}

Twilight Echo declarative theme scaffold.

Use `structured` token IDs for stable visual overrides. `variables` and `theme.css`
remain available for advanced compatibility styling.

```bash
npm install
npm test
npm run pack
```
